<h1><%= @publisher.name %></h1>
<hr>
<h2><%= @publisher.name %> Games</h2>
<ul>
  <% @products.each do | product | %>
    <li> <%= link_to product.name, product %></li>
  <% end %>
</ul>

<%= paginate @products %>
